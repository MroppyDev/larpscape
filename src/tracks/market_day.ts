// 'Market Day' — exported from stupie.flp (user composition).
// Plays through the bundled OSRS GM soundfont via MIDI sequencer.
import { Track } from './notation';

export const track: Track = {
  name: 'Market Day',
  bpm: 120,
  loopBars: 0,
  channels: [],
  midiUrl: '/music/market_day.mid',
};
